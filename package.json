{
  "name": "expo-router-monorepo",
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "plus": "yarn lernaupdate --lazy",
    "clean": "yarn lernaupdate --lazy --dedupe"
  },
  "devDependencies": {
    "lerna-update-wizard": "^1.1.2",
    "prettier": "^2.5.1",
    "turbo": "^1.3.1"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "private": true
}
